name: workflow

on:
  push:
    branches: [ main ]
  pull_request:

jobs:
  build-test-analyze:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
